---
- name: Setup database
  hosts: db
  tasks:
    - name: Run database playbook
      include_role:
        name: db

- name: Setup web server
  hosts: server
  tasks:
    - name: Run web server playbook
      include_role:
        name: server
